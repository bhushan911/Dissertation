/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ["pisces.bbystatic.com", "localhost"],
  },
};

module.exports = nextConfig;
